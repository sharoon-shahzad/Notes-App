<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes Taking App</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/javascripts/script.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-900 via-purple-900 to-gray-900 min-h-screen text-white">
    <nav class="bg-gray-800 p-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">Notes Taking App</h1>
            <ul class="flex space-x-6">
                <li><a href="#" class="hover:text-gray-300">Home</a></li>
                <li><a href="#" class="hover:text-gray-300">Features</a></li>
                <li><a href="#" class="hover:text-gray-300">About</a></li>
                <li><a href="#" class="hover:text-gray-300">Contact</a></li>
            </ul>
        </div>
    </nav>
    <header class="text-center py-10">
        <h2 class="text-4xl font-bold mb-4">Welcome to the Notes Taking App</h2>
        <p class="text-lg max-w-2xl mx-auto">Our app helps you organize your thoughts and tasks efficiently. Create, read, update, and delete your notes with ease, ensuring your productivity is at its peak. Experience the convenience of managing your notes in a secure and user-friendly environment.</p>
    </header>
    <div class="main w-full flex flex-col items-center">
        <div class="form w-full flex justify-center items-center">
            <div class="w-full max-w-md bg-gray-800 p-8 rounded-lg shadow-lg">
                <h2 class="text-white text-3xl font-bold mb-6 text-center">Add New Note</h2>
                <form action="/create" method="post">
                    <div class="form-group mb-4">
                        <label for="title" class="block text-gray-300 mb-2">Title</label>
                        <input type="text" name="title" id="title" class="w-full p-3 rounded-md bg-gray-700 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500">
                    </div>
                    <div class="form-group mb-6">
                        <label for="details" class="block text-gray-300 mb-2">Description</label>
                        <textarea name="details" id="details" cols="30" rows="5" class="w-full p-3 rounded-md bg-gray-700 text-white border border-gray-600 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter your description"></textarea>
                    </div>
                    <button class="btn w-full p-3 rounded-md bg-blue-600 text-white font-semibold hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500" type="submit" value="submit">Add Note</button>
                </form>
            </div>
        </div>
        <div class="tasks flex justify-center items-center flex-wrap gap-5 p-10">
            <% if(files.length > 0){ %>
                <% files.forEach((file)=>{ %>
                    <div class="task min-w-72 px-6 py-4 bg-gray-800 rounded-lg shadow-lg">
                        <h1 class="text-white text-2xl font-semibold mb-4"><%= file %></h1>
                        <div class="flex w-full justify-between items-center">
                            <a class="text-blue-400 hover:text-blue-500 text-lg" href="/notes/<%= file %>">Read more</a>
                            <a href="/edit/<%= file %>"><button class="text-blue-400 hover:text-blue-500 text-lg">Edit</button></a>
                            <a href="/delete/<%= file %>"><button class="text-red-400 hover:text-red-500 text-lg">Delete</button></a>
                        </div>
                    </div> 
                <% }) %>
           <% } else { %>
            <h3 class="text-white text-2xl">No task added yet!</h3>
            <% } %>
        </div> 
    </div>
</body>
</html>
